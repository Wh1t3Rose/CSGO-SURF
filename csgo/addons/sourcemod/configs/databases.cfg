"Databases"
{
	"driver_default"		"sqlite" // set to mysql and change for every plugin below if using MySQL
	
	// When specifying "host", you may use an IP address, a hostname, or a socket file path

	
	"storage-local"
	{
		"driver"		"sqlite" // Change to MySQL if needed.
		"host"			"localhost" // Set External IP of DB Server if MySQL DB is on a different Server.
		"database"		"sourcemod_storage" // Don't change if using SQLITE.
		"user"			"root" // Leave as root if using SQLITE
		"pass"			"" // Leave blank if using SQLITE
		//"port"		"3306" // leave commented if using SQLITE. Port only apples to MySQL
		//"timeout"		"300" Uncomment if experience excessive database write times
	}

	"clientprefs"
	{
		"driver"		"sqlite"
		"host"			"localhost"
		"database"		"clientprefs"
		"user"			"root"
		"pass"			""
		//"port"		"3306"
	}

	"cksurf"
	{
		"driver"		"sqlite"
		"host"			"localhost"
		"database"		"cksurf"
		"user"			"root"
		"pass"			""
		//"port"		"3306"
	}

	"store"
	{
	    	"driver"        	"sqlite"
	    	"host"          	"localhost"
	    	"database"      	"store"
	    	"user"          	"root"
	    	"pass"          	""
	   	//"port"		"3306"
		
	}

	"togsclantags"
	{
		"driver"		"sqlite"
		"host"			"localhost"
		"database"		"tog"
		"user"			"root"
		"pass"			""
	}
} 
